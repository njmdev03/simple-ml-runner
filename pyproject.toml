[tool.uv.workspace]
members = [
    "packages/simple-config",
    "packages/simple-registries",
    "packages/core",
    "packages/cli",
]

[dependency-groups]
dev = [
    "pyfakefs>=6.2.0",
    "pytest>=9.0.3",
]
