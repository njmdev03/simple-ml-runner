[tool.uv.workspace]
members = [
    "packages/simple_config",
    "packages/simple_registries",
    "packages/ml_runner_core",
    "packages/ml_runner_cli",
]

[dependency-groups]
dev = [
    "pyfakefs>=6.2.0",
    "pytest>=9.0.3",
]
