[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ml_runner_cli"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "argparse>=1.4.0",
]

[project.scripts]
ml-runner = "ml_runner_cli.cli:main"
